# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "der-parser"
version = "3.0.4"
authors = ["Pierre Chifflier <chifflier@wzdftpd.net>"]
include = ["LICENSE-*", "README.md", ".gitignore", ".travis.yml", "Cargo.toml", "bench/*.rs", "src/*.rs", "src/ber/*.rs", "src/der/*.rs", "tests/*.rs"]
description = "Parser for BER/DER encoded data"
homepage = "https://github.com/rusticata/der-parser"
readme = "README.md"
keywords = ["BER", "DER", "ASN1", "parser", "nom"]
categories = ["parser-implementations"]
license = "MIT/Apache-2.0"
repository = "https://github.com/rusticata/der-parser.git"
[package.metadata.docs.rs]
all-features = true
features = ["bigint"]
[dependencies.nom]
version = "5.0"

[dependencies.num-bigint]
version = "0.2"
optional = true

[dependencies.rusticata-macros]
version = "2.0.2"
[dev-dependencies.hex-literal]
version = "0.2"

[dev-dependencies.pretty_assertions]
version = "0.6"

[features]
bigint = ["num-bigint"]
default = []
[badges.travis-ci]
repository = "rusticata/der-parser"
